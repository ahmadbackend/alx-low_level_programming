#include <stdio.h>
#include <string.h>
/**
 * main - entry point
 *@agrc: number of arguments
 *@argv: vector of arguments
*/
int main(int argc, char *argv[])
	{
		printf("%s\n", argv[argc-argc]);
		return (0);
	}
