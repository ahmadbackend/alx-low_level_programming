#include <stdio.h>
#include <string.h>
/**
 * main - entry point
 *@argc: number of arguments
 *@argv: vector of arguments
 * Return: always success
*/
int main(int argc, char *argv[])
	{
		printf("%s\n", argv[argc - argc]);
		return (0);
	}
