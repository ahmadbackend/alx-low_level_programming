/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
	{
		char test = 'a';

		for (test = 'a' ; test < 'z' ; test++)
		{
			putchar(test);
			putchar('\n');
		}
		return (0);
	}
